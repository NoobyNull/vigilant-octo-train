---
phase: 35-gcodepanel-rendering-elimination
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/panels/gcode_panel.h
  - src/ui/panels/gcode_panel.cpp
  - src/app/application.cpp
autonomous: true
requirements: [ELM-01, ELM-02, ELM-03]

must_haves:
  truths:
    - "GCodePanel does not instantiate or own a Renderer, Camera, Framebuffer, or Shader"
    - "GCodePanel still provides statistics, file info, CNC sender controls, and file open/save functionality"
    - "GCodePanel render() shows only text/stats/sender UI -- no 3D viewport, no ImGui::Image() call"
    - "No GL resource leaks from removed VAO/VBO/shader objects"
    - "Application main loop no longer calls gcodePanel->updateSimulation()"
    - "The build compiles cleanly and all tests pass"
  artifacts:
    - path: "src/ui/panels/gcode_panel.h"
      provides: "GCodePanel class without any rendering members or method declarations"
      contains: "class GCodePanel"
    - path: "src/ui/panels/gcode_panel.cpp"
      provides: "GCodePanel implementation without rendering functions"
    - path: "src/app/application.cpp"
      provides: "Main loop without gcodePanel->updateSimulation() call"
  key_links:
    - from: "src/ui/panels/gcode_panel.cpp"
      to: "src/ui/panels/gcode_panel.h"
      via: "Header declares only kept members/methods"
      pattern: "No Renderer|Camera|Framebuffer|Shader members"
    - from: "src/app/application.cpp"
      to: "src/ui/panels/gcode_panel.h"
      via: "No call to updateSimulation on gcodePanel"
      pattern: "!gcodePanel.*updateSimulation"
---

<objective>
Strip all 3D rendering infrastructure from GCodePanel now that ViewportPanel handles all viewport rendering (Phases 31-34).

Purpose: Eliminate duplicated rendering code. GCodePanel originally contained its own Renderer, Camera, Framebuffer, path geometry, simulation overlay, and mouse handling. All of this has been rebuilt in ViewportPanel across Phases 31-34. The GCodePanel rendering code is now dead weight.

Output: A significantly smaller gcode_panel.h/cpp that retains only its text-based role: file loading, statistics display, CNC sender controls, and callbacks.
</objective>

<execution_context>
@/home/matthew/.claude/get-shit-done/workflows/execute-plan.md
@/home/matthew/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-simulation-playback/34-01-SUMMARY.md
@src/ui/panels/gcode_panel.h
@src/ui/panels/gcode_panel.cpp
@src/app/application.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip rendering infrastructure from GCodePanel header and implementation</name>
  <files>src/ui/panels/gcode_panel.h, src/ui/panels/gcode_panel.cpp</files>
  <action>
Remove all 3D rendering infrastructure from GCodePanel. This is a surgical removal -- delete code, do not add new features.

**gcode_panel.h changes:**

1. Remove these includes (no longer needed):
   - `#include <glad/gl.h>`
   - `#include "../../render/camera.h"`
   - `#include "../../render/framebuffer.h"`
   - `#include "../../render/renderer.h"`

2. Remove the `SimState` enum (lines 34) -- simulation now lives in ViewportPanel's VPSimState.

3. Remove the `updateSimulation(float dt)` public method declaration (line 90).

4. Remove these private method declarations:
   - `renderPathView()`
   - `renderZClipSlider()`
   - `renderSimulationControls()`
   - `buildPathGeometry()`
   - `destroyPathGeometry()`
   - `handlePathInput()`
   - `renderLiveDro()`

5. Remove these private member variables and related blocks:
   - View settings block: `m_currentLayer`, `m_maxLayer`, `m_showRapid`, `m_showCut`, `m_showPlunge`, `m_showRetract`, `m_colorByTool`
   - The entire `toolColor()` static method and `NUM_TOOL_COLORS` constant
   - 3D rendering pipeline block: `m_pathRenderer`, `m_heightLineShader`, `m_pathCamera`, `m_pathFramebuffer`, `m_pathViewWidth`, `m_pathViewHeight`, `m_pathDirty`, `m_needsCameraFit`
   - Path geometry block: `m_pathVAO`, `m_pathVBO`, `m_rapidStart/Count`, `m_cutStart/Count`, `m_plungeStart/Count`, `m_retractStart/Count`
   - Simulation overlay block: `m_simVAO`, `m_simVBO`
   - ToolGroup struct and `m_toolGroups` vector
   - Previous filter state block: `m_prevShowRapid`, `m_prevShowCut`, `m_prevShowPlunge`, `m_prevShowRetract`, `m_prevLayer`
   - Simulation state block: `m_simState`, `m_simTime`, `m_simSpeed`, `m_simTotalTime`, `m_simSegmentIndex`, `m_simSegmentProgress`, `m_segmentTimes`, `m_segmentTimeCumulative`

**gcode_panel.cpp changes:**

1. Remove these includes (no longer needed after rendering removal):
   - `#include "../../render/gl_utils.h"`
   - `#include "../../render/shader_sources.h"`

2. Constructor (line 67-75): Remove `m_pathRenderer.initialize()`, `m_heightLineShader.compile(...)`, `m_pathCamera.reset()`, `m_pathCamera.setYaw()`, `m_pathCamera.setPitch()`. Keep only `Panel("G-code")` init and `m_availablePorts = listSerialPorts()`.

3. Destructor (line 77-79): Remove `destroyPathGeometry()` call. The destructor body becomes empty (or can be `= default` if declared in header).

4. `render()` method (lines 81-200): Restructure the main content area:
   - Remove the `if (m_mode == GCodePanelMode::View) { renderSimulationControls(); }` block (lines 151-153). The View mode no longer has simulation controls here (they're in ViewportPanel toolbar).
   - Replace the two-layout "PathView" child window (which called `renderPathView()`) with a G-code text listing. The new "GCodeListing" child window should show a scrollable list of G-code lines using ImGui::ListClipper for performance:
     ```cpp
     ImGui::BeginChild("GCodeListing", ImVec2(0, 0), true);
     if (hasGCode()) {
         ImGuiListClipper clipper;
         clipper.Begin(static_cast<int>(m_program.commands.size()));
         while (clipper.Step()) {
             for (int i = clipper.DisplayStart; i < clipper.DisplayEnd; ++i) {
                 const auto& cmd = m_program.commands[static_cast<size_t>(i)];
                 bool isAcked = m_cncConnected && i <= m_lastAckedLine;
                 if (isAcked)
                     ImGui::PushStyleColor(ImGuiCol_Text, ImVec4(0.3f, 0.8f, 0.3f, 1.0f));
                 ImGui::Text("%6d  %s", i + 1, cmd.raw.c_str());
                 if (isAcked)
                     ImGui::PopStyleColor();
             }
         }
         if (m_scrollToLine >= 0 && m_scrollToLine < static_cast<int>(m_program.commands.size())) {
             float lineH = ImGui::GetTextLineHeightWithSpacing();
             ImGui::SetScrollY(static_cast<float>(m_scrollToLine) * lineH);
             m_scrollToLine = -1;
         }
     }
     ImGui::EndChild();
     ```
   - In the wide layout (availWidth >= 420): Stats on the left (with console below in Send mode as before), GCodeListing on the right.
   - In the narrow layout (availWidth < 420): Stats on top (40% height), GCodeListing below.

5. `loadFile()` (lines 216-282): Remove these lines that are only relevant to the old 3D view:
   - Remove `m_maxLayer = m_program.boundsMax.z;` and `m_currentLayer = m_maxLayer;`
   - Remove the segment times computation block (lines 245-257: `m_segmentTimes.resize(...)` through `m_simTotalTime = ...`)
   - Remove `m_pathDirty = true;` and `m_needsCameraFit = true;`
   - Keep everything else (file reading, parsing, stats analysis, repo lookup, callback firing).

6. `clear()` (lines 284-303): Remove:
   - `m_currentLayer = 0.0f;` and `m_maxLayer = 100.0f;`
   - `destroyPathGeometry();` and `m_pathDirty = true;`
   - `m_simState = SimState::Stopped;` through `m_segmentTimeCumulative.clear();`
   - Keep: `m_program = {}`, `m_stats = {}`, `m_filePath.clear()`, `m_currentGCodeId = -1`, `m_lastAckedLine = -1`, `m_streamProgress = {}`, callback firing.

7. `reanalyze()` (lines 518-547): Remove the segment times rebuild block (lines 527-538) and the simulation state reset block (lines 541-544). Keep: the Analyzer call and `m_stats = analyzer.analyze(m_program)`. Remove `m_pathDirty = true;` at the end.

8. Delete these entire functions (they are 100% rendering code):
   - `renderSimulationControls()` (lines 931-997)
   - `updateSimulation()` (lines 999-1043)
   - `buildPathGeometry()` (lines 1047-1211)
   - `destroyPathGeometry()` (lines 1212-1230)
   - `handlePathInput()` (lines 1232-1301)
   - `renderPathView()` (lines 1303-1564)
   - `renderZClipSlider()` (lines 1566-1573)
   - `renderLiveDro()` (lines 1963-1994)

After all removals, the file should drop from ~1996 lines to roughly ~1100-1200 lines.
  </action>
  <verify>
    <automated>cd /data/DW && cmake --build build -j$(nproc) 2>&1 | tail -5</automated>
  </verify>
  <done>
    - gcode_panel.h has no Renderer, Camera, Framebuffer, Shader, or GL members
    - gcode_panel.cpp has no renderPathView, buildPathGeometry, destroyPathGeometry, handlePathInput, renderZClipSlider, renderSimulationControls, updateSimulation, or renderLiveDro functions
    - Constructor no longer initializes rendering objects
    - Destructor no longer destroys path geometry
    - render() shows stats and G-code text listing, no ImGui::Image() call
    - Build succeeds with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove updateSimulation wiring and verify full build</name>
  <files>src/app/application.cpp</files>
  <action>
Remove the per-frame `gcodePanel()->updateSimulation()` call from the application main loop.

In `src/app/application.cpp` around line 457-459, remove these lines:
```
// Update simulation in gcode panel each frame
if (m_uiManager && m_uiManager->gcodePanel())
    m_uiManager->gcodePanel()->updateSimulation(ImGui::GetIO().DeltaTime);
```

The viewport panel's `updateSimulation()` call (lines 461-463) must remain -- that's the active simulation system.

After removal, verify:
1. Full build succeeds
2. All tests pass
3. gcode_panel.cpp line count has dropped significantly (target: under 1200 lines)
  </action>
  <verify>
    <automated>cd /data/DW && cmake --build build -j$(nproc) 2>&1 | tail -5 && ctest --test-dir build --output-on-failure 2>&1 | tail -5 && wc -l src/ui/panels/gcode_panel.cpp src/ui/panels/gcode_panel.h</automated>
  </verify>
  <done>
    - application.cpp no longer calls gcodePanel->updateSimulation()
    - ViewportPanel's updateSimulation() call is preserved
    - Build compiles cleanly with zero errors
    - All tests pass
    - gcode_panel.cpp is under 1200 lines (down from 1996)
  </done>
</task>

</tasks>

<verification>
1. `cmake --build build -j$(nproc)` -- compiles cleanly
2. `ctest --test-dir build --output-on-failure` -- all tests pass
3. `grep -c "Renderer\|Camera\|Framebuffer\|Shader\|GLuint\|m_pathVAO\|m_simVAO" src/ui/panels/gcode_panel.h` -- returns 0
4. `grep -c "renderPathView\|buildPathGeometry\|destroyPathGeometry\|handlePathInput\|renderZClipSlider\|renderSimulationControls\|updateSimulation\|renderLiveDro" src/ui/panels/gcode_panel.cpp` -- returns 0
5. `grep -c "gcodePanel.*updateSimulation" src/app/application.cpp` -- returns 0
6. `wc -l src/ui/panels/gcode_panel.cpp` -- under 1200 lines
</verification>

<success_criteria>
- GCodePanel has zero 3D rendering capability -- no Renderer, Camera, Framebuffer, Shader, VAO, VBO, or GL calls
- GCodePanel retains all non-rendering functionality: file loading, statistics, CNC sender, job history, search/goto, callbacks
- Application main loop no longer drives GCodePanel simulation
- gcode_panel.cpp line count reduced by ~800+ lines (from ~1996 to under 1200)
- All tests pass, build is clean
</success_criteria>

<output>
After completion, create `.planning/phases/35-gcodepanel-rendering-elimination/35-01-SUMMARY.md`
</output>
